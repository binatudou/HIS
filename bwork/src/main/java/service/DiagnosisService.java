package service;

import java.sql.SQLException;
import java.util.List;

import bean.Diagnosis;
import bean.Drugs;
import bean.RegistForm;
import dao.DiagnosisDao;
import dao.DrugsDao;

public class DiagnosisService {
    public static void diagnose(Diagnosis diagnosis) throws SQLException, ClassNotFoundException {
        // DiagnosisDao dDao = new DiagnosisDao();

        // List<RegistForm> rFormList = RegistFormService.findById(diagnosis.getRegistID());
        //     dDao.insert(diagnosis);

        // dDao.close();
        // return lPriceList;
    }
}